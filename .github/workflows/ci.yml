---
name: CI
'on':
  pull_request:
  push:
    branches:
      - main

jobs:
  gather-metadata:
    uses: GrabAByte/github-devops-workflows/.github/workflows/writeMetadata.yml@main
    with:
      metadataFile: 'metadata.json'
      metadataPath: .
  docker-lint:
    uses: GrabAByte/github-devops-workflows/.github/workflows/dockerLint.yml@main
    with:
      dockerfilePath: 'docker/Dockerfile'
  docker-build:
    uses: GrabAByte/github-devops-workflows/.github/workflows/dockerBuild.yml@feat/dockerBuild
    with:
      dockerfilePath: 'docker/Dockerfile'
    needs: [gather-metadata, docker-lint]
